apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: vault-test-secret
  namespace: vault-eso-app
spec:
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: test-secret
  data:
  - secretKey: FOO
    remoteRef:
      key: test-secret
      property: FOO
  - secretKey: SOURCE
    remoteRef:
      key: test-secret
      property: SOURCE